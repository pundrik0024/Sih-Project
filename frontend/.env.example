# Production: set this in the Vercel project's environment variables.
# Include the backend's API version prefix, and do not add a trailing slash.
VITE_API_BASE_URL=https://unishield-ri4l.onrender.com/api/v1

# Local development: leave this unset so Vite proxies /api to localhost:8000.
